//"use strict"; used to treat all js code as new version of js
const accname = "Druv"
var accNo = 23
let accEmail = "abc@gmail.com"

console.table({accname, accNo, accEmail})
console.log(`My name is ${accname} and my account number is ${accNo}`)
// alert(3+3) cannot run in node, can run in browser